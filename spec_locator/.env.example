# Spec Locator Service - 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# ===== 数据目录配置 =====
# 规范文件索引目录（必需）
SPEC_DATA_DIR=D:\projects\liuzong\output_pages

# 上传文件目录（可选）
SPEC_UPLOAD_DIR=D:\projects\liuzong\spec_locator\uploads

# 临时文件目录（可选）
SPEC_TEMP_DIR=D:\projects\liuzong\spec_locator\temp

# 日志目录（可选）
SPEC_LOG_DIR=D:\projects\liuzong\spec_locator\logs

# ===== API 服务配置 =====
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# ===== 日志配置 =====
LOG_LEVEL=INFO
DEBUG=false

# ===== OCR 配置 =====
OCR_USE_GPU=false
OCR_PRECISION=fp32
OCR_CONF_THRESHOLD=0.3

# OCR 懒加载配置（性能优化）
OCR_LAZY_LOAD=true              # 是否启用懒加载（默认true，首次使用时才加载模型）
OCR_WARMUP_ON_STARTUP=false     # 启动时后台预热（默认false，推荐生产环境设为true）

# ===== 大模型配置 =====
# LLM识别开关
LLM_ENABLED=true

# LLM提供商选择: doubao/openai/gemini
LLM_PROVIDER=doubao

# API密钥配置（根据提供商选择对应的密钥）
# 豆包 (字节跳动)
DOUBAO_API_KEY=your_doubao_api_key_here
# OpenAI (ChatGPT)
OPENAI_API_KEY=your_openai_api_key_here
# Google Gemini
GEMINI_API_KEY=your_gemini_api_key_here

# 模型名称（留空使用默认值）
# Doubao默认: doubao-vision-pro
# OpenAI默认: gpt-4o
# Gemini默认: gemini-1.5-pro
LLM_MODEL=

# 性能配置
LLM_TIMEOUT=30                  # API超时时间（秒）
LLM_MAX_RETRIES=2               # 最大重试次数

# 混合模式配置
LLM_AUTO_FALLBACK=true          # 自动降级到OCR
OCR_CONFIDENCE_THRESHOLD=0.6    # OCR置信度阈值（低于此值时切换到LLM）

# Prompt版本
LLM_PROMPT_VERSION=v1
