# =============================================================================
# Spec Locator Service - 环境变量配置示例
# =============================================================================
# 将此文件复制为 .env 并填入真实的配置值
# 在AWS部署时，可以通过环境变量或 .env 文件配置

# -----------------------------------------------------------------------------
# 数据目录配置
# -----------------------------------------------------------------------------
SPEC_DATA_DIR=/app/data/output_pages
SPEC_UPLOAD_DIR=/app/uploads
SPEC_TEMP_DIR=/app/temp
SPEC_LOG_DIR=/app/logs

# -----------------------------------------------------------------------------
# API 服务配置
# -----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8002
API_WORKERS=4

# -----------------------------------------------------------------------------
# 日志配置
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
DEBUG=false

# -----------------------------------------------------------------------------
# OCR 配置
# -----------------------------------------------------------------------------
OCR_USE_GPU=false
OCR_PRECISION=fp32
OCR_CONF_THRESHOLD=0.3
OCR_LAZY_LOAD=true
OCR_WARMUP_ON_STARTUP=true

# -----------------------------------------------------------------------------
# LLM 大模型配置（新增）
# -----------------------------------------------------------------------------
# 是否启用LLM识别
LLM_ENABLED=true

# LLM提供商：doubao/openai/gemini
LLM_PROVIDER=doubao

# API密钥（根据所选提供商配置对应的密钥）
# 豆包（火山引擎）
DOUBAO_API_KEY=your-doubao-api-key-here
# OpenAI
OPENAI_API_KEY=your-openai-api-key-here
# Google Gemini
GEMINI_API_KEY=your-gemini-api-key-here

# 模型名称（根据提供商设置）
# 豆包: doubao-vision-pro
# OpenAI: gpt-4o, gpt-4-turbo, gpt-4-vision-preview
# Gemini: gemini-1.5-pro, gemini-1.5-flash
LLM_MODEL=doubao-vision-pro

# 性能配置
LLM_TIMEOUT=30
LLM_MAX_RETRIES=2

# 混合模式配置
LLM_AUTO_FALLBACK=true
OCR_CONFIDENCE_THRESHOLD=0.6

# Prompt版本
LLM_PROMPT_VERSION=v1

# -----------------------------------------------------------------------------
# 系统配置
# -----------------------------------------------------------------------------
TZ=Asia/Shanghai
PYTHONIOENCODING=utf-8
